# Локальный полный стек: см. deploy/combined/docker-compose.full.yml
# Скопируйте в `.env` при необходимости переопределить значения по умолчанию.
#
POSTGRES_DB=coordinator_db
POSTGRES_USER=coordinator
POSTGRES_PASSWORD=coordinator

# По умолчанию — учебный координатор (тот же хост, что и у отдельного воркера в deploy/worker).
COORDINATOR_URL=http://194.87.98.244:8080
#
# Только локальный full-stack без выхода в интернет на 194 — раскомментируйте строку ниже и закомментируйте 194:
# COORDINATOR_URL=http://coordinator:8080
